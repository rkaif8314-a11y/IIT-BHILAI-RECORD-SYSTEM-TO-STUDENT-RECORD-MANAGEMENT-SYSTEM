"use client";
import { FormEvent,useState } from "react";
import { signInWithEmailAndPassword } from "firebase/auth";
import { doc,getDoc } from "firebase/firestore";
import { auth,db } from "@/lib/supabase";
export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[message,setMessage]=useState("");async function submit(e:FormEvent){e.preventDefault();setMessage("Signing in…");try{const c=await signInWithEmailAndPassword(auth,email,password);const p=await getDoc(doc(db,"profiles",c.user.uid));const role=p.exists()?p.data().role:"student";window.location.href=role==="admin"?"/admin":role==="faculty"?"/faculty":"/student";}catch(err){setMessage(err instanceof Error?err.message:"Unable to sign in.");}}return <main className="page"><nav className="nav"><div className="brand"><a href="/" style={{color:"inherit",textDecoration:"none"}}>←</a><div className="logo">A</div><div>ASRS<small>Firebase secure sign in</small></div></div></nav><section style={{maxWidth:520,margin:"0 auto",padding:"70px 20px"}}><span className="eyebrow">ASRS Authentication</span><h1 style={{fontSize:48,letterSpacing:"-.04em"}}>Sign in</h1><p style={{color:"#9db0c7"}}>Use your registered ASRS account.</p><form onSubmit={submit} className="panel"><label>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required style={input}/></label><label style={{display:"block",marginTop:16}}>Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" required style={input}/></label><button className="button primary" style={{marginTop:20,cursor:"pointer",width:"100%"}}>Sign in</button>{message&&<p style={{color:"#9db0c7"}}>{message}</p>}</form></section></main>};
const input={display:"block",width:"100%",marginTop:8,padding:13,borderRadius:12,background:"#0c1b2d",color:"#fff",border:"1px solid rgba(255,255,255,.12)"};